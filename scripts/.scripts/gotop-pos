#!/bin/sh
if [[ $1 = "-v" ]]; then
urxvtc -name "vtop" -title "vtop" -e sh -c "vtop --theme certs;zsh"
elif [[ $1 = "-h" ]]; then
urxvtc -name "htop" -title "htop" -e sh -c "htop;zsh"
else
urxvtc -name "gotop" -title "gotop" -geometry 74x42 -e "gotop"
fi
